# ══════════════════════════════════════════════════════════════════════════════
# KAI BUNDLE - ENVIRONMENT CONFIGURATION
# ══════════════════════════════════════════════════════════════════════════════
#
# Copy this file to ~/.claude/.env and fill in your values.
# This file documents all available environment variables for the Kai bundle.
#
# SECURITY: Never commit your actual .env file. It contains secrets.
# ══════════════════════════════════════════════════════════════════════════════

# ──────────────────────────────────────────────────────────────────────────────
# IDENTITY (Set during installation)
# ──────────────────────────────────────────────────────────────────────────────

# Your AI assistant's name
DA="Kai"

# Your timezone (used for timestamps, scheduling)
TIME_ZONE="America/New_York"

# ──────────────────────────────────────────────────────────────────────────────
# VOICE TTS CONFIGURATION
# ──────────────────────────────────────────────────────────────────────────────
# The voice server supports cascading TTS with automatic fallback:
#   ElevenLabs (if configured) → Piper (local) → macOS say (built-in)

# Primary TTS provider: 'elevenlabs' | 'piper' | 'macos'
# - 'piper': Free, local neural TTS (recommended)
# - 'elevenlabs': High-quality cloud TTS (has usage limits)
# - 'macos': Built-in macOS voices (always works)
TTS_PROVIDER="piper"

# Voice server port (default: 8888)
# PAI_VOICE_PORT="8888"

# Pre-speech chime notification
# VOICE_CHIME_ENABLED="true"
# VOICE_CHIME_PATH="~/.claude/voice-server/chime.mp3"

# ── ElevenLabs (Cloud TTS) ────────────────────────────────────────────────────
# Get API key from: https://elevenlabs.io/api
# Free tier: ~10,000 characters/month

# ELEVENLABS_API_KEY="sk_..."
# ELEVENLABS_VOICE_ID="CwhRBWXzGAHq8TQ4Fs17"

# ── Piper (Local Neural TTS) ──────────────────────────────────────────────────
# Piper is auto-installed during setup. No API key needed.
# Models: https://huggingface.co/rhasspy/piper-voices

# Custom model path (optional - default model auto-downloaded)
# PIPER_MODEL_PATH="~/.claude/voice-server/piper-models/en_US-lessac-medium.onnx"

# ── macOS say (Built-in) ──────────────────────────────────────────────────────
# List available voices: say -v ?
# Popular voices: Samantha, Alex, Victoria, Daniel

# MACOS_VOICE="Samantha"

# ──────────────────────────────────────────────────────────────────────────────
# CORE PLATFORM TOKENS (Optional - for extended functionality)
# ──────────────────────────────────────────────────────────────────────────────
# These enable Claude Code to interact with external services.
# Only add the ones you need.

# ── GitHub ────────────────────────────────────────────────────────────────────
# Create token: https://github.com/settings/tokens
# Scopes: repo, read:org (for full functionality)

# GITHUB_TOKEN="ghp_..."

# ── Vercel ────────────────────────────────────────────────────────────────────
# Create token: https://vercel.com/account/tokens

# VERCEL_API_TOKEN="..."

# ── Supabase ──────────────────────────────────────────────────────────────────
# Get token: https://supabase.com/dashboard/account/tokens

# SUPABASE_ACCESS_TOKEN="sbp_..."

# ── Cloudflare ────────────────────────────────────────────────────────────────
# Create token: https://dash.cloudflare.com/profile/api-tokens
# Recommended template: "Edit Cloudflare Workers"

# CLOUDFLARE_API_TOKEN="..."
# CLOUDFLARE_ACCOUNT_ID="..."

# ── Sentry ────────────────────────────────────────────────────────────────────
# Create token: https://sentry.io/settings/account/api/auth-tokens/

# SENTRY_AUTH_TOKEN="sntryu_..."
# SENTRY_ORG="your-org"

# ── OpenAI ────────────────────────────────────────────────────────────────────
# Create key: https://platform.openai.com/api-keys

# OPENAI_API_KEY="sk-proj-..."

# ── Google Cloud ──────────────────────────────────────────────────────────────
# Service Account: https://console.cloud.google.com/iam-admin/serviceaccounts

# GOOGLE_APPLICATION_CREDENTIALS="/path/to/service-account.json"
# GOOGLE_PROJECT_ID="your-project"

# OAuth (for user sign-in flows):
# GOOGLE_CLIENT_ID="...apps.googleusercontent.com"
# GOOGLE_CLIENT_SECRET="GOCSPX-..."

# ──────────────────────────────────────────────────────────────────────────────
# MCP SERVER CONFIGURATION (Optional)
# ──────────────────────────────────────────────────────────────────────────────
# Model Context Protocol servers extend Claude Code's capabilities.
# Configure in ~/.claude/settings.json, but some need env vars.

# Example: Browserbase MCP (headless browser automation)
# BROWSERBASE_API_KEY="..."
# BROWSERBASE_PROJECT_ID="..."

# Example: Neon MCP (serverless Postgres)
# NEON_API_KEY="..."

# ──────────────────────────────────────────────────────────────────────────────
# HOOKS CONFIGURATION (Optional)
# ──────────────────────────────────────────────────────────────────────────────

# Session history output directory
# PAI_HISTORY_DIR="~/.claude/history"

# Enable/disable specific hooks (all enabled by default)
# HOOK_VOICE_ENABLED="true"
# HOOK_HISTORY_ENABLED="true"
# HOOK_SECURITY_ENABLED="true"

# ──────────────────────────────────────────────────────────────────────────────
# SECURITY & DEBUGGING
# ──────────────────────────────────────────────────────────────────────────────

# Enable debug logging for hooks
# DEBUG="true"

# Security validator strictness: 'strict' | 'warn' | 'off'
# SECURITY_MODE="strict"
